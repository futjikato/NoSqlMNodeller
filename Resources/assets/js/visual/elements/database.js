define(["visual/elements/element"], function(Element) {
    Database.prototype = new Element();
    Database.prototype.constructor = Database;

    function Database() {

    }

    return Database;
});